<div class="content-divider-preview">
    @if($separator)
        <span class="content-divider-preview-separator">{{ strtoupper($separator) }}</span>
    @endif
    <div class="content-divider-preview-heading">{{ $heading }}</div>
    <div class="content-divider-preview-line"></div>
</div>
