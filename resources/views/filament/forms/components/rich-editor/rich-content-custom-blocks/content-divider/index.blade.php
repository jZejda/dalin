<div class="content-divider-block">
    @if($separator)
        <span class="content-divider-separator">{{ strtoupper($separator) }}</span>
    @endif
    <h2 class="content-divider-heading">{{ $heading }}</h2>
    <div class="content-divider-line"></div>
</div>
