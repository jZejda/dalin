<div class="hero-preview">
    <div class="hero-preview-content">
        <h3 class="hero-preview-heading">{{ $heading }}</h3>
        @if($subheading)
            <p class="hero-preview-subheading">{{ $subheading }}</p>
        @endif
    </div>
</div>
