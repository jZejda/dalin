# NODE Dependencies
node_modules/
package-lock.json

# Public dirs
/public/build
/public/hot
/public/storage
/public/orisdata
/public/media
/storage/*.key

/bootstrap/cache

# Environment files
.env
.env.backup
# Repo je veřejné — žádné další .env varianty s reálnými hodnotami
.env.*
!.env.example
!.env.demo.example
!.env.testing
exclude_file*
.scribe
.phpunit.result.cache

_ide_helper_models.php
_ide_helper.php
Homestead.json
Homestead.yaml
auth.json

# Logs
npm-debug.log
yarn-error.log

# IDE files
/.idea
/.vscode
/.scribe
/.cursor
/.junie
.phpstorm.meta.php
CLAUDE.md

# OS files
.DS_Store
Thumbs.db

# PHP
vendor/

# Deploy
# deploy.php obsahuje hostnames a SSH uživatele produkčních hostingů,
# repo je veřejné → zůstává mimo git (sdílet ručně / přes password manager).
deploy.php
deploy.sh
# Per-site tajnosti pro `dep config:upload` (.deploy/<alias>/.env, site-config.php)
.deploy/

# MCP local config (contains API keys)
.mcp.json

# Deployer zapisuje do release adresáře git SHA nasazeného kódu
/REVISION
